{
  "name": "@design-mode/mcp-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted multi-tenant MCP server for Design Mode — deploys to Vercel, relays between coding agents and the browser extension via Upstash Redis Streams.",
  "license": "MIT",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@upstash/redis": "^1.34.3",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vercel": "^39.0.0"
  }
}
